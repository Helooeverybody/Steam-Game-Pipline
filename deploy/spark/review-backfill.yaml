apiVersion: "sparkoperator.k8s.io/v1beta2"
kind: SparkApplication
metadata:
  name: spark-review
  namespace: default
spec:
  type: Python
  pythonVersion: "3"
  mode: cluster
  image: "frostedpilot/spark:finalv3"
  sparkVersion: "3.5.4"

  mainApplicationFile: "local:///opt/spark/app/raw_clean_reviews.py"

  restartPolicy:
    type: Never

  sparkConf:
    "spark.sql.shuffle.partitions": "8"

  driver:
    cores: 1
    memory: "1g"
    serviceAccount: spark-operator-spark
    env:
      - name: PYSPARK_PYTHON
        value: /usr/bin/python3
      - name: SPARK_VERSION
        value: "3.5"
      - name: BACKFILL
        value: "true"
    volumeMounts:
      - name: ivy-cache
        mountPath: /tmp

  executor:
    cores: 1
    instances: 2
    memory: "1g"
    volumeMounts:
      - name: ivy-cache
        mountPath: /tmp
